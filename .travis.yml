language: php
php:
    - 5.3

before_script:
  - curl -s https://getcomposer.org/installer | php
  - php composer.phar install

script: phpunit --configuration resources/tests/phpunit.xml --bootstrap resources/tests/phpunitbootstrap.php
